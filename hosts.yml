all:
    children:
        cloud_1_server:
            hosts:
                server1:
                    ansible_host: is-wis.com

                # Add more servers to deploy to all of them in one run.
                # Each host needs a matching host_vars/<name>/ directory
                # (copy host_vars/server2/ as a starting point).
                # server2:
                #     ansible_host: example2.com
